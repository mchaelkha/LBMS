package Book;

import java.util.Comparator;

public class CopiesComparator implements Comparator<BookInfo> {
    @Override
    public int compare(BookInfo o1, BookInfo o2) {
        return 0;
    }
}
