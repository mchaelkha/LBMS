package Visitor;

public class InLibrary implements VisitorState{

    @Override
    public void beginVisit(VisitorDB visitorDB) {
        //String visitorID = visitorDB.
        //return "arrive,"+;
        //Error: A visitor with the id is already visiting the library
    }
}
